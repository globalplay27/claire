services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: claire
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - claire_pgdata:/var/lib/postgresql/data

volumes:
  claire_pgdata:
